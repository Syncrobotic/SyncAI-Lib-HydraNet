# 專案維護稽核：2026-09-16

本輪暫停新增功能與正式模型訓練，檢查文件時效、測試有效性、品質門檻與工作程序
完整性。修正不改動已凍結的標籤、checkpoint、歷史圖片或原始審核紀錄。
完整性驗證與程式測試不代表 StudioA 模型或 3D 場景已通過現場驗收。

## 發現與修正

起始版本 `abd34da` 的完整 CPU 測試為 **25 failed、4,093 passed、20 skipped**。
失敗原因包括 15 項幾何測試的 loader 替身未跟上 `plate_path` 介面、2 項歷史圖片
新鮮度檢查、1 項重複 IGNORE 定義、5 項將中文 UI 字串誤判為實作語言違規、
1 項整份檔案 Unicode noqa，以及 1 項測試模組互相匯入檢查。

| 問題 | 最終處理與保護 |
| --- | --- |
| 測試替身過時、測試間交叉匯入 | 替身明確檢查新參數；共用資料建構器移入 `_fixture` helper；幾何計算邏輯不變 |
| 訓練 worker 只看 completed 報告就跳過 | 核對 job 雜湊、必要輸出、路徑及各產物 SHA-256；拒絕損壞、外來 job、錯誤評估範圍與缺檔 |
| 中斷可能留下半份狀態 JSON | 同目錄暫存、flush/fsync 後原子替換；失敗保留原檔並清理暫存；不宣稱完整斷電交易保證 |
| 比較流程只有介面測試不足以驗證訓練 | 新增實際 CPU Trainer 測試：兩次更新、權重改變、initial state、驗證產物，且禁止建立 test dataset |
| 覆蓋率漏算 52 個巢狀工具模組 | 納入 namespace directories；工具／腳本分母由 59 個模組補齊至 111 個 |
| 84.6% 可被整數顯示當成 85% 過關 | 改由 coverage 原生精確門檻判定；測試確認低於門檻、缺資料都失敗；85%／11% 門檻不變 |
| 型別 gate 未監看依賴鎖變化 | hook 納入 `uv.lock`；tools 既有診斷上限由 73 收緊至實際 70 |
| 中文顯示與英文實作規約混在一起 | 以 token／AST 檢查識別字、註解與 docstring；保留中文 UI／報告；新增正反例測試，移除整檔 noqa |
| README 舊 GIF 被讀成目前效果 | 明示 2026-09-11 歷史快照；歷史例外同時綁定圖片與原 audit 雜湊及相鄰告示；原隱私／有效內容檢查繼續執行 |
| 沒有私有資料就跳過整份文字契約測試 | 純文字檢查每次執行；只有依賴本機產物的數值比較可跳過；跳過上限仍為 25 |
| 多份文件各自聲稱「目前／下一步」 | 建立單一進度與產物索引；標示歷史批次、已解除的 GCS 登入阻礙、舊 taxonomy 與訓練範圍 |

GCS「121 張 mask 保留」相對的是 v3；與保留模型使用的 v2 比較則有一張手機標籤
變化。文件已寫清楚比較基準。未找到目前程式／設定／部署對 `hydranet_fti_stage0`
舊路徑的依賴；仍在使用的 BEV 座標與幾何功能不能僅因為是 2D 表示就判成過時。

## 驗證證據

完整結果與檔案雜湊見 [機器可讀稽核紀錄](reviews/project_audit_20260916.json)。
本機原始輸出位於 `runs/project_audit_20260916_v1/`，不隨 Git 發布。

| 檢查 | 結果 | 受測版本 |
| --- | --- | --- |
| 完整本機 CPU | 4,163 passed、12 skipped、0 failed；退出 0 | `3509411` |
| CUDA 路徑補驗 | 60 passed、0 skipped；退出 0 | `3509411` |
| 乾淨副本完整回歸 | 4,152 passed、24 skipped、0 failed；退出 0 | `db95e6f` |
| 可攜性與品質 guard | 539 passed | `db95e6f` |
| 乾淨副本覆蓋率（含分支） | src **87.16%**／scripts + tools **12.27%**；85%／11% 正式 gate 通過 | `db95e6f` |
| Ruff | check 通過、497 個檔案 format 通過 | 最終程式樹 |
| 型別 ratchet | 8／14／70，符合各自上限 | `3509411` |
| Bandit | src medium+、scripts/tools high+：0 findings、0 scan errors | `3509411` |
| 相對文件連結 | 0 個損壞的 repository 連結；23 個指向 gitignored 本機產物 | 稽核文件範圍 |

第一次乾淨副本測試雖然沒有失敗案例，仍因 28 個 skip 超過上限而退出 1；
此結果保留，未算作成功。修正文字契約的整檔 skip 後新增一項獨立產物比較，
最終總數為 4,176，24 項 skip，符合原有 25 上限。CPU 的 12 項 GPU skip
由獨立 CUDA 補驗覆蓋；乾淨副本其餘 skip 來自缺少私有產物的整合檢查。

CPU 完整回歸與 CUDA 補驗使用同一個既有鎖定環境。乾淨副本沒有私有 runs、
datasets、weights，透過明確 PYTHONPATH 載入該副本程式，但沿用主工作區的 venv；
這不是全新依賴安裝、淺層 Git clone 或遠端 CI。套件版本與各次受測 commit 均留存。
型別 gate 使用隔離 Python 3.12；pytest 使用 Python 3.11.15。

保留語意模型的 6 個輸出、偵測模型的 8 個輸出與三組資料 manifest 已核對雜湊。
這證明本次檢查的產物完整性，不能證明標籤正確或模型達標。

## 剩餘界線與後續工作

- 型別檢查仍有 **92 項既有診斷**：src 8、scripts 14、tools 70；本輪符合不退步門檻，並非零型別錯誤。
- 工具／腳本覆蓋率仍偏低；本輪補正計算分母與關鍵失敗路徑，後續應優先補高影響 worker／資料轉換測試。
- Bandit 只按目前 CI 的 severity 範圍檢查；未做新的依賴漏洞公告稽核。
- 未重新執行遠端 GitHub CI、Python 3.12／3.13 完整測試矩陣或完整 ONNX 匯出矩陣。
- 沒有重新生成歷史 GIF。當時含未提交程式，無法聲稱由單一 Git commit 精確重現；頁面明確保留此限制。
- Stage1 驗收與 Stage2–4 現場驗證仍未完成；本輪不執行新的 test 推論、不升級正式模型。

下一項模型工作仍是 AI 修整玻璃／門評估標籤與柱子缺例，再凍結訓練比較。
目前保持暫停，詳見 [StudioA 依序執行進度](STUDIOA_EXECUTION_PROGRESS.md)。

## 原子提交

- `c96e67c`：測試介面、共用 fixture 與依賴 hook。
- `fc805f9`：worker 產物驗證、原子狀態寫入與實際 CPU 執行測試。
- `3509411`：覆蓋率／型別門檻、歷史圖表契約與文件時效。
- `db95e6f`：乾淨副本也執行文字契約測試。

以上為本機提交，未推送遠端。稽核紀錄以受測程式版本為準；後續報告提交只補文件與註解。
